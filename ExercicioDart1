void main() {
  var nome = 'Camiseta ADS';
  var quantidade = 3;
  var preco = 49.90;
  bool disponivel = true;
  const imposto = 5.0;

  var precoTotal = (quantidade * preco) * (1 + imposto / 100);

  print('Produto: ' + nome);
  print('Disponivel em Estoque: ' + disponivel.toString());
  print('Quantidade;' + quantidade.toString());
  print('Preço unitario: ' + preco.toString());
  print('Valor Total: R\$' + precoTotal.toString());
}
